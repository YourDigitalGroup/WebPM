# Copy to .env before building. Vite bakes these into dist/ at build time,
# so a build made without them ships an app that throws on load.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
